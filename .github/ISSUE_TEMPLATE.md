<!-- Rough template — fill in what's relevant, skip what isn't. -->

**What happened?**

**Repro steps**

**OS + Claude Code version**
(e.g. macOS 14.4, `claude --version` output)

**Output of `python3 ~/.claude/commands/lib/clean_sessions.py stats`** (optional)
```
paste here, scrub sensitive paths
```
